data:
  dataset: libras_move
  dataset_url: https://huggingface.co/datasets/MLLab-TS/libras_move/resolve/main/dataset.csv
  target_column: target
experiment:
  tags:
  - libras_move
  name: libras_move
model:
  type: RandomForestClassifier
  n_estimators: 20
  min_samples_split: 12
  min_impurity_decrease: 0.0
  max_features: 0.3
  max_depth: 8
  criterion: entropy

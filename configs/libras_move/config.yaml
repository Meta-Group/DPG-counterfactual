# Configuration for libras_move dataset
# Only dataset-specific overrides are included here.
# Base defaults are loaded from configs/config.yaml
#
# Usage:
#   python scripts/run_experiment.py --dataset libras_move --method dpg
#   python scripts/run_experiment.py --dataset libras_move --method dice

data:
  dataset: libras_move
  dataset_path: configs/libras_move/dataset.csv
  target_column: target
experiment:
  tags:
  - libras_move
  name: libras_move

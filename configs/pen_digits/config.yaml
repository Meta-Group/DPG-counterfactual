# Configuration for pen_digits dataset
# Only dataset-specific overrides are included here.
# Base defaults are loaded from configs/config.yaml
#
# Usage:
#   python scripts/run_experiment.py --dataset pen_digits --method dpg
#   python scripts/run_experiment.py --dataset pen_digits --method dice

data:
  dataset: pen_digits
  dataset_path: configs/pen_digits/dataset.csv
  target_column: target
experiment:
  tags:
  - pen_digits
  name: pen_digits

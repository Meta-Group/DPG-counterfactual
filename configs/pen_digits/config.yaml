data:
  dataset: pen_digits
  dataset_url: https://huggingface.co/datasets/MLLab-TS/pen_digits/resolve/main/dataset.csv
  target_column: target
experiment:
  tags:
  - pen_digits
  name: pen_digits
model:
  type: RandomForestClassifier
  n_estimators: 20
  min_impurity_decrease: 0.0
  max_features: 0.5
  max_depth: 10
